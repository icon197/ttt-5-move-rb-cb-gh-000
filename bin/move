#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
puts "Welcome to Tic Tac Toe!"
puts "Where would you like to go?"
val = input_to_index(gets.strip)
board = Array.new(9, " ")
# move(board, val)
display_board(board)
